---
description: Deferred — restyle notifications header (Events filter is wired)
globs: closet/Shared/NotificationsView.swift
alwaysApply: false
---

# Deferred: notifications header restyle

**Status:** Partial. `NotificationsView` has **All / Items / Outfits / Events / Friends** filter; `event_invite` rows show under **Events** with Accept/Decline.

## Goal when revisiting

1. **Restyle the notifications header** (title + filter row): improve visual hierarchy, spacing, and picker presentation (SelectionHeader-style or similar). Five segments may need a scrollable or menu-style filter on small screens.
2. Preserve unread/badge behavior and deep links.

## When done

Remove this rule.
