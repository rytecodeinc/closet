---
description: Deferred — remaining calendar worn-history work (outfit, windowed badge, history list)
globs: closet/Shared/SocialEngagementActionsRow.swift,closet/Item View/ItemDetailView.swift,closet/Item View/ItemWearDetailsSheet.swift,closet/Outfit Views/OutfitDetailView.swift,closet/Services/CoreDataHelpers.swift,closet/Outfit Views/Calendar Views/**/*.swift
alwaysApply: false
---

# Deferred: remaining calendar worn stats

**Status:** Partially done. Owned closet `ItemDetailView` calendar icon / worn count opens `ItemWearDetailsSheet`. Badge count is still lifetime `pastWornEvents` (not a rolling window). `onCalendar` is still a no-op on outfits.

## Still parked

1. Align badge count with a product window (30 days vs lifetime vs calendar year) if desired.
2. Optional full previously-worn history list / deep-link to the event.
3. Same sheet on `OutfitDetailView`.
4. Empty-state calendar (show icon at 0 wears) if the sheet should exist without history.

## When done

Remove this rule.
